#include<iostream>
#include<cstdio>
#include<algorithm>
#include<vector>
#include<string>
#include<cmath>
#include<stack>
#include<queue>
#include<set>
#include<map>
using namespace std;
int main()
{
	int n, m;
	cin >> n >> m;
	int a;
	pair<int, int>p;
	set<pair<int, int>>st;
	for (int i = 0; i < n; i++)
	{
		cin >> p.first;
		p.second = 1;
		st.insert(p);
		cin >> p.first;
		p.second = -1;
		st.insert(p);
	}
	vector<int>v1(1e9+1);
	while (st.size() > 0 && st.begin()->first<=0)
	{
		v[i]
	}
}